{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "update-check",
  "description": "Permission for the main window to check for updates in the background",
  "platforms": ["macOS", "windows"],
  "windows": ["main"],
  "permissions": ["updater:allow-check"]
}
