{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updates",
  "description": "Updater permissions limited to the settings window",
  "platforms": ["macOS", "windows"],
  "windows": ["settings"],
  "permissions": ["updater:default", "process:allow-restart"]
}
