{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "status",
  "description": "Minimal permissions for the live status window",
  "windows": ["status"],
  "permissions": [
    "core:event:default",
    "core:window:allow-hide",
    "core:window:allow-show"
  ]
}
